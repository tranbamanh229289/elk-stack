server:
  port: 8083

spring:
  application:
    name: query-service
  cloud:
    config:
      name: query-service
      profile: default
      uri: http://localhost:8000

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761