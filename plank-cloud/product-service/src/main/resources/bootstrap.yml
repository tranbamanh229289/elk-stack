server:
  port: 8082

spring:
  application:
    name: product-service
  cloud:
    config:
      name: product-service
      profile: default
      uri: http://localhost:8000

eureka:
  client:
    service-uri:
      default-zone: http://localhost:8761